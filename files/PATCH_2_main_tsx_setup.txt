/*
 * PATCH 2 — Setup next-themes ThemeProvider di main.tsx
 *
 * CARA PASANG:
 * Buka file: client/src/main.tsx
 * Lakukan dua perubahan berikut:
 */


// ── STEP 1: Tambahkan import ini di bagian atas main.tsx ──────
import { ThemeProvider } from "next-themes";


// ── STEP 2: Wrap <App /> dengan <ThemeProvider> ───────────────
//
// SEBELUM (kira-kira begini):
//
//   ReactDOM.createRoot(document.getElementById("root")!).render(
//     <React.StrictMode>
//       <QueryClientProvider client={queryClient}>
//         <App />
//       </QueryClientProvider>
//     </React.StrictMode>
//   );
//
//
// SESUDAH (tambahkan ThemeProvider di dalamnya):
//
//   ReactDOM.createRoot(document.getElementById("root")!).render(
//     <React.StrictMode>
//       <ThemeProvider
//         attribute="class"       // <-- tambahkan class "dark" ke <html>
//         defaultTheme="dark"     // <-- default gelap (sesuai desain landing page)
//         enableSystem={true}     // <-- ikuti setting OS user
//         disableTransitionOnChange
//       >
//         <QueryClientProvider client={queryClient}>
//           <App />
//         </QueryClientProvider>
//       </ThemeProvider>
//     </React.StrictMode>
//   );


// ── PENJELASAN attribute="class" ─────────────────────────────
//
// Saat user aktifkan dark mode, next-themes akan tambahkan
// class "dark" ke tag <html>:
//
//   <html class="dark">...</html>
//
// Ini yang "dibaca" oleh Tailwind dan index.css:
//   @custom-variant dark (&:is(.dark *))  ← sudah ada di index.css
//   .dark { --background: ... }           ← yang kita tambahkan di Patch 1
//
// Toggle dark mode dari komponen mana pun bisa pakai:
//   import { useTheme } from "next-themes";
//   const { theme, setTheme } = useTheme();
//   setTheme("dark")  // atau "light" atau "system"
